# ML-Project-SMS-Spam-Classifier
# This projects is a sms spam classification web built with machine learning and streamlit that classifies whether a sms is spam or not spam in real time
# To install requirements - "pip install -r requirements.txt"
# To run the app - "streamlit run app.py"